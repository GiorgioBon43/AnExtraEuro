html
    head
        title AnExtraEuro 
        script(src="https://cdn.tailwindcss.com")
    body
        div(style="display: flex; flex-direction: column; align-items: center; white-space: pre-line;")
            h1 Le tue Campagne
        div(style="position: absolute; top: 10px; right: 10px;")
            p(style="color: red; font-weight: bold;") #{data}
        div(style="position: absolute; top: 50px; right: 10px;")
            button(onclick=`home()`) Torna alla home 
        div(id="myCampains")

    script(type="module" src="loginAndRegister/js/myCampains.js")

    script. 
        function home() {
            fetch('/login/log', {
                method: 'GET',
            }).then(() => {
                window.location.href = '/';
            });
        }

