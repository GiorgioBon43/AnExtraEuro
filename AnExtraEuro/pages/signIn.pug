doctype html
html
    head
        title AnExtraEuro
        script(src="https://cdn.tailwindcss.com")
    body
        form
            label(for="username")
                i.fas.fa-user
                input(type="text", name="username", placeholder="Username", required)

            label(for="password")
                i.fas.fa-lock
                input(type="password", name="password", placeholder="Password", required)

            label(for="confermaPassword")
                i.fas.fa-lock
                input(type="password", name="confermaPassword", placeholder="Conferma password", required)

            label(for="email")
                i.fas.fa-user
                input(type="text", name="email", placeholder="Email", required)

            input(type="submit", value="SingIn")
        
        div(style="position: fixed; top: 10px; right: 10px; color: red; font-weight: bold")
            button(onclick="home()") Home 
            
        script(src="loginAndRegister/js/signIn.js")
